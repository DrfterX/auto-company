# 2026-06-06

## 项目全面审查 + P0/P1 修复

### 审查
- 从四角色（架构/工程/QA/产品）角度审查了 Auto Company 项目
- 发现了 9 个问题，按 P0/P1/P2 分级
- 用户澄清：实验项目（非商业）、硬编码路径是故意喂给AI的期货系统、已有双API切换逻辑

### P0 修复（scripts/core/auto-loop.sh）
- **API 健康检查**：每轮循环前 curl 探测 8082 代理，不可达则跳过本轮
- **指数退避**：失败后 10s→20s→40s→80s→160s cap 300s，替代固定 30s 重试
- **Next Action 死锁检测**：连续 3 轮不变→强制注入 pivot 指令打破死循环
- **status.json 输出**：每轮结束写入结构化 JSON（cycle/status/cost/errorType/nextAction/revenue/users/idleSkipCount）

### P1 修复
- **auto-loop.conf**：api_key 改为环境变量引用 ${DEEPSEEK_API_KEY}
- **dashboard/server.py**：新增 read_status_json()，在 gather_status_payload 中注入 cycleStatus
- **dashboard/app.js**：renderStateList 展示 cycleStatus 字段（Cycle Status/Cost/Error Type/Next Action/Idle Skips 等）

### 关于 Skill 能力
- 项目无自主创建 skill 的能力。.claude/skills/ 只有一个 frontend-design.md
- README 声称的 30+ 专家是 agent prompt（.claude/agents/），不是 skill

## 智能额度管理系统

### 三层额度检测
1. **API 错误响应检测**：SenseNova 返回 402/429 或 body 中含 quota/额度/insufficient 关键词 → 自动判定额度耗尽
2. **Token 累计计数**：每次 API 调用的 input/output tokens 持久化到 `.quota-state.json`，支持跨 session 累积
3. **5 小时重置窗口**：耗尽后计算下次重置时间，auto-loop 自动休眠等待

### 修改的文件
- `scripts/core/sensenova-proxy.py` — 全面重写：使用 SENSE_NOVA_API_KEY（不再转发请求 Key）、Token 计数、额度检测、/quota 端点
- `scripts/core/auto-loop.sh` — health check 改为查询 /health 端点，新增 QUOTA 状态处理
- `scripts/core/start-dual-api.sh` — 启动时自动从 ~/.hermes/config.yaml 提取 SENSE_NOVA_API_KEY
- `dashboard/app.js` — 新增 Quota 状态展示

## 最终审查修复（Bash 3.2 兼容性）

### 发现的运行时 Bug
1. `local backoff_seconds` 在函数外使用 — bash 3.2 会在运行时报错，已修复
2. `$(( expr ? val1 : val2 ))` 三元运算 — bash 3.2 不支持，已全部替换为 if-else
3. `.quota-state.json` 等新状态文件未 gitignore — 已添加

### 最终状态
- 所有语法检查通过（bash -n + py_compile）
- 兼容 macOS 默认 bash 3.2
- 6个任务全部完成

### 多模型智能路由 v2
- 重写 sensenova-proxy.py: 10模型角色感知路由+双计数器+错误日志
- 10模型全部可用(1 SN + 9 MS)
- 错误日志: .model-errors.jsonl (分析真实限速)
- 修复 auto-loop.sh 残留中文占位文本
