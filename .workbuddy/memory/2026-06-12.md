# 2026-06-12

## 04:36 — 定时任务：恢复第1个 SenseNova Key，重启 proxy

## 10:08~10:40 — 修复 Cycles #26~#27 共识丢失问题 + 强制任务拆解规则

### 问题背景
- Cycle #26 (09:15~09:45) Timed out — 实际修改了 `auth.ts` + 构建了 `cli.js`，但共识回退
- Cycle #27 (09:45~10:05) exit 143 — 实际修改了 `db.ts`/`index.ts`/`types.ts`，共识再次回退
- 共识丢失导致后续 Cycle 不知道磁盘上已有改动

### 做了什么
1. 通过文件 mtime 审计恢复 Cycles #26~#27 的真实产出，更新 `memories/consensus.md`
2. 强化 PROMPT.md 规则 #8（Plan-first → 强制任务拆解），新增 Next Action 格式规范
3. consensus.md 的 Next Action 区域改用子任务清单格式，Step 4 示范拆分 4 个子 Cycle
4. Cycle #28 随后自主确认 Step 2 已完成，并推进了 Step 3（Free tier 限制），部署上线

### 规则变更
- **旧规则 #8**: 复杂任务才 Plan（条件触发）
- **新规则 #8**: 强制任务拆解 — 每个 Step 必须拆成 ≤20min 的子任务清单
- 杀掉旧 proxy (port 8082)
- 启动新 proxy，加载全部 5 个 SENSE_NOVA_API_KEY 轮换
- 健康检查通过，`"keys": 5`

## 10:59 — 合并健康监控（自动化执行）

**时段:** 清醒时段 (08:00~23:59)
**状态:** ✅ 健康

### 检查结果
- **Auto Loop:** 运行中 (PID 75730/74600), LOOP_COUNT=30, ERROR_COUNT=0
- **Cycle #30 进行中** (11:07:10 启动)
- **最近 2 小时 Cycles:** #26 FAIL(Timeout)→自动恢复, #27 FAIL(exit 143)→自动恢复, #28 OK(Timeout+进度保留), #29 OK(Timeout+进度保留)
- **PM2 options-trading:** online (PID 20841, 37h, 2 restarts)
- **sensenova-proxy:** running (PID 8422)
- **sensenova-router:** running (PID 51823)
- **dual-api-proxy:** running (PID 76078)
- **status.json:** cycle=29, status=OK, exitCode=124
- **monitor-alerts.log:** 最近 2h 内无 ISSUES FOUND 或 ALERT 记录

## 12:14 — 手动暂停 Auto Company + 监控面板修复

### 操作
- 修复监控面板"停止"按钮无效 bug（auto-loop.sh 第 931/799 行 + app.js 改进）
- 强制终止所有 auto-loop/claude 进程（waiting_limit 状态下 sleep 3600 导致停止信号不生效）
- 创建暂停标志 `.auto-loop-paused`，设置 4.5 小时后自动恢复（16:44）
- 新建一次性自动化任务 "Auto Company 暂停4.5小时后自动恢复"

### 原因
5 个 SenseNova API 账号额度接近耗尽，需暂停 4.5 小时等额度重置。

## 16:44~16:56 — 自动化任务执行：Auto Company 自动恢复

### 操作
1. 删除暂停标志文件 `.auto-loop-paused`
2. 启动 auto-loop：`bash scripts/core/auto-loop.sh` 后台运行（PID 16483）
3. 写入恢复记录到 `notification-last.txt`
4. 写入 PID 到 `.auto-loop.pid`

### 注意
- 首次启动因 `.auto-loop.pid` 被 nohup shell PID 占用导致脚本误判为已有实例而退出
- 修正：先清空 `.auto-loop.pid`，让脚本自行管理 PID 文件后成功启动
- Auto-loop Cycle #1 于 16:56:13 开始执行，Engine=claude, 3 个 bash 进程在线

## 21:22~22:10 — 健康监控 + 额度消耗分析 + PROMPT.md 主矛盾修复

### 健康监控
- **状态:** ✅ 健康
- **Auto Loop:** 运行中 (PID 16483), LOOP_COUNT=11, ERROR_COUNT=1
- Cycle #10 FAIL (21:27 Timeout, 已自动恢复共识), Cycle #11 进行中
- 最近 2h 无 ISSUES FOUND/ALERT 记录

### 额度消耗分析
- **结论：空转不是元凶，13 小时 Timeout 循环才是**
- 51 次成功 Cycle（~449s 有效耗时）+ 24 次 Timeout（46,800s = 13h 连续烧 API）
- 160 次 SKIP（0 消耗）、17 次快速失败（cost=0）几乎不影响额度
- 软超时 42%（有产出但跑太久）vs 硬超时 58%（纯白烧）
- LIMIT 触发 2 次：6/11 23:32 和 6/12 11:16

### 超时根因 5 种模式
1. **🎯 超额执行（主因）** — AI 在一个 cycle 里做了计划外的事
2. **🔄 重启热身税** — 每次恢复后 5-7 个 cycle 白烧
3. **🥚 共识 restore 后失忆循环** — 回滚旧共识导致重复失败
4. **🫤 大步未拆到位** — Step 5.5 ~2h 超标准
5. **🌐 代理层延迟** — sensenova-proxy 翻译链

### PROMPT.md 修复（核心矛盾）
- 第 2 节 "决策" → 改为「严守范围」，明确只执行当前一步
- "Ship > Plan > Discuss" → 加了解释：态度优先级≠范围指导，不允许超额
- 第 4 节 "执行" → 增加「硬性边界」+「快速失败规则」：5 分钟无产出就 exit 1
- 收敛规则第 4 条 → 改为「只产出一件实物」，超额也违规
- 收敛规则第 9 条（新增）→ 防 Scope Creep：提前收手不算偷懒，超额完成算违规

### auto-loop.sh 优化
- 默认超时从 1800s（30 分钟）→ **900s（15 分钟）**，最大浪费减半
- 新增**热身模式**：每次启动后的前 3 个 cycle 超时降至 **300s（5 分钟）**，快速跳过"重新读上下文"的热身阶段
- 启动日志加上了 warmup 配置显示
- 修复**共识 restore 后失忆循环**：restore 后 Next Action 自动标记 RETRY #1/#2，第 3 次强制 pivot；成功 cycle 或 breaker 复位时清除 retry 计数
- 新增**子任务粒度自动校验** `check_subtask_granularity()`：每个 cycle 启动前解析 Next Action 表格，检测 ≥20min 的子任务，向 consensus 插入报警提醒
- **砍掉 sensenova-proxy 翻译层**：Claude CLI 直连 `https://token.sensenova.cn`（Anthropic 原生协议），移除 600 行 proxy.py、key 轮换、health check poll、QUOTA handler 等全部冗余逻辑
- **新增智能 Key 轮换** `rotate_api_key()` + `mark_key_exhausted()`：维护 6 个 Key，额度用完自动标记时间戳并切换，5h 冷却后自动恢复；状态文件 `.key-rotation-state`
- **全 Key 耗尽处理机制**：6 个 Key 全部用完时自动计算最早恢复时间，chunked sleep 等待（每 60s 重试检查），避免空转重试
