# 2026-06-18 工作日志

## 00:49 — 自动化健康监控（睡眠时段）
- 执行 health check，追加快照至 monitor-alerts.log
- Auto Loop RUNNING (PID 11088), Cycle #106~#108 全部 429 限流
- 睡眠时段静默运行

## 01:09 — API Key 切换机制失效 Bug 排查与修复
阿勇发现 6 个 Key 切换机制完全失效，日志里始终只用 kaixin5227，与实际控制台不一致。

### 根因分析
1. **Bug 1 (致命)**: `auto-loop.sh` 中 `mark_key_exhausted()` 定义了但从未被调用。429 错误处理只调 `rotate_api_key()`，但因为 `.key-rotation-state` 所有 key 的 timestamp 均为 0，rotate 永远选第一个 key（kaixin5227）。Cycle #92~#112 连续 20 次全是 kaixin5227。
2. **Bug 2**: `sensenova-proxy.py` 只加载 5 个 key（缺 `_6`），注释也写的是"3个Key"。
3. **Bug 3**: `start-dual-api.sh` 在加载 `.env` 之前就启动了 proxy，导致 proxy 启动时缺少 `_2`~`_6` 环境变量。

### 修复
- `auto-loop.sh` line 1282: 429 处理增加 `mark_key_exhausted` 调用（在 `rotate_api_key` 之前）
- `sensenova-proxy.py` line 25-32: 添加 `SENSE_NOVA_API_KEY_6`，注释更新为6个
- `start-dual-api.sh`: 重构启动顺序 — `.env` 加载移至步骤0（proxy 启动之前），移除重复的 `.env` 加载
- 重启服务验证（PID 81457，后续 01:20 再次重启确认6 Key，01:32 第三次重启）

### 副作用注意
- ~~当前 proxy 仍是旧启动顺序下启动的（只有5 key），下次重启即可全部修复~~ → 01:20 已重启，Proxy 确认 Keys: 6 ✅

## 01:27 — 共识时间戳精度修复
- 根因：模型经常不跑 `date` 命令，直接复制旧时间戳
- 修复：在 auto-loop.sh 新增 `stamp_consensus_timestamp()` 函数，cycle 成功后由 bash 覆写 `## Last Updated`
- 两个成功出口均注入调用（soft timeout + normal success）

## 01:31 — 429 时保留部分工作进度
- 根因：429时无条件 restore_consensus，哪怕 agent 已经写了部分共识
- 修复：429 分支不再无条件 restore。先检查 `validate_consensus && consensus_changed_since_backup`，两个都过就保留（+ stamp timestamp），否则才 restore
- 非429错误保持原有无条件 restore 行为
- 01:32 重启，PID 84436

## 08:30 — P2 Critiq 代码审查任务接入（human consensus）
- 构建并配置 Critiq CLI（SenseNova deepseek-v4-flash / projects/critiq/dist/cli.js）
- 审查范围：options_arbitrage_system（git diff 增量）、scripts/、critiq 自审查
- **首次审查发现 3 个问题：**
  - 🔴 runtime.txt → python-3.13.12 部署可能不兼容（warning）
  - 🔴 futures_dashboard.html:1193 → K-line interval 清理不彻底，popup 关闭时未清理 `_currenCell` interval，visibilitychange 重启引用已移除 DOM（CRITICAL）
  - 🟡 futures_dashboard.html:1226 → visibilitychange 重启 interval 前缺少 DOM 有效性校验（warning）

## 09:21 — 修复期货K线浮窗ABC标注 + 期权IV柱状图压缩问题

### 期货 K 线浮窗
- **问题**：只有周线 ABC 标注正确，其他周期没标或标错；A→B→C 线段未绘制（之前是虚线太淡）
- **根因**：`/api/klines` 调用 `restructure_active_for_symbol` 时未强制全量重算，短周期 N 型结构未检测
- **修复**：
  1. `app.py:679` — `restructure_active_for_symbol` 加 `force_full_recalc=True`
  2. `futures_dashboard.html` — A→B 和 B→C 线段改为**实线**（2px, 85% opacity），之前是`[4,4]`虚线
  3. C→最新价第三笔方向线改为**实线+箭头**（之前是`[6,4]`虚线）

## 09:38 — K线浮窗整体放大 + 附图 MACD(12,26,9)
- 浮窗宽度 380→520px，canvas 高度 160→340px
- 布局：220px K线主图 + 100px MACD 附图 + 20px 间隔
- MACD：EMA12-EMA26=DIF（白线），DIF的9日EMA=DEA（黄线），柱状（红涨绿跌）
- 零轴虚线，MACD(12,26,9) 标签，calcEMA() 辅助函数
- 定位计算：适配 540×450 浮窗，移动端 max-height 55vh→70vh
  4. 无 ABC 数据时 popup 显示 "N 型结构待确认（数据不足以标记ABC）" 而非空白

## 13:14 — 综合代码审查报告（合并外部审查）
- 读取并验证外部 Agent 在 `报告.md` 中的审查发现
- 验证结果：确认 Stripe Webhook 字段映射 CRITICAL bug（subscription_id 匹配 customer_id 列）、SSL 禁用+CORS*+0.0.0.0 安全三连、Critiq Worker process.env 崩溃、savePR 死代码
- 外部 B4（风控SHORT止损）、B10（Bearer双重前缀）被驳回——代码逻辑合理
- 合并产出最终报告：44 个问题（P0×8 / P1×15 / P2×10 / P3×11）
- 报告文件：`.workbuddy/plans/swift-nebula-newton.md`

## 14:10 — 共识更新：新增人类任务（N型算法 + IV柱状图）
- 更新 `consensus.md` User Directives 区域
- **P0（续）**：N型结构算法在所有周期统一修复 — 先判方向（上升/下降）再标点，每个周期5条判定条件，所有周期算法一致
- **P1（续）**：IV百分位柱状图改进 — 柱体再拉长 + X轴显示品种中文名（但不压缩柱子）

### 期权 IV 百分位柱状图
- **问题**：柱状图整体挤在上部，柱体被严重压缩
- **根因**：ECharts `grid.bottom: 280` 吃掉几乎全部绘图区域 + `.iv-section` 有 `max-height: 55vh` 限制
- **修复**：
  1. `options_dashboard.html:1069` — `grid.bottom: 280` → `80`（释放 200px 绘图空间）
  2. `options_dashboard.html:84` — 移除 `max-height: 55vh`，`min-height: 45vh` → `50vh`
  3. X 轴标签简化：单行显示品种代码（原两行符号+中文名，中文名放 tooltip），rotation 35°→30°
- 共识新增 `🤖 P2 — Critiq 代码审查` 章节（User Directives 区域）
- 修复优先级：低于 Phase 3-A，Critical 在 Phase 结束后安排
