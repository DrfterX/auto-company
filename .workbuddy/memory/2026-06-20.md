## 2026-06-20

### Auto Company 共识维护与 N 型标注不一致问题设定

1. **清理期权面板旧共识** — 从 consensus.md User Directives 中删除：
   - P1 信号与数据（DTE/OI/保证金补全、SmartFilter 错误耦合、Telegram Markdown）
   - Completed Tasks Log 中 4 条期权面板相关条目
   - P1 后端 Iron Condor max_loss 条目
   - Active Projects / Next Action 中期权面板引用
   - 保留 P1 前端/后端代码质量条目（echarts/ innerHTML / finally:pass / float(None)）

2. **新增 P0 问题 — 期货面板各周期 N 型标注不一致**：
   - 现象：部分周期标对、部分标错、部分未标
   - 核心矛盾：算法统一但结果不统一 → 问题在落地差异不在算法定义
   - 6 个自主排查方向：数据源一致性 / 算法调用路径 / 缓存重算时机 / K 线数据量差异 / 浮窗渲染层 / 品种覆盖差异

3. **设定 Next Action** — 从空转 41 轮改为全量审计任务，第一步：数据源一致性审计

4. **Railway 备份机制上线**：
   - Railway 已运行：`optionsarbitragesystem-production.up.railway.app` (Online)
   - 本机: `127.0.0.1:5100` (主用), Railway (停电备用)
   - GitHub auto-deploy: DrfterX/options-arbitrage-system → Railway webhook
   - 脚本: `scripts/core/railway-sync.sh` (同步), `scripts/core/backup-check.sh` (状态监控)
   - 共识已记录备份机制

5. **期权面板新原则设定**：
   - 只计算主力合约对应的期权链，非主力合约跳过
   - 原因：流动性差、算力浪费
   - 6 项实现要求写入共识
   - Next Action 设定为主力合约判定逻辑实现
