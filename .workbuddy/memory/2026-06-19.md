## 2026-06-19

### 自动化修改
- 更新了 Auto Company 健康监控自动化（automation-1781070851802）的 prompt
- 新增「步骤 3：项目活动摘要报告」— 每次检查时从 auto-loop.log 提取自上次检查以来的 Cycle SUMMARY 条目，生成项目做了什么事的总结报告

### MACD 修复
- KLINE_COUNT 60→200 回退，改为 API 返回 200 bars(计算用) + display_count=60(显示用)
- 前端 drawKline 新增 displayCount 参数，MACD 切片对齐显示窗口

### 共识清理
- 删除已完成的 P1（旧期权UI/UX）和 P1（续）IV柱状图章节
- 删除过时的"P0 待 P1 完成后启动"执行顺序规则

### 审计报告汇总
- 阅读 4 份审计报告：53项综合 + 29项options/signals + 23项前端 + P0修复指令
- 提取 P0(5项) + P1(10项) 更新至 consensus.md User Directives
- P0: SSL/CORS/监听、Stripe字段映射、硬编码密码+Token、CSS语法、Canvas缩放
- P1: 信号推送补DTE/OI/保证金、SmartFilter耦合、Telegram Markdown、echarts判空、innerHTML转义、dashboard缺MACD、hub.py finally:pass、Iron Condor max_loss、iv_recorder、float(None)

### MACD 修复
- 发现 K 线浮窗 MACD 显示不正常
- **根因**：`KLINE_COUNT = 60`，MACD(12,26,9) 需要 26 根 seed EMA + 9 根 seed DEA = 35 根，60 根中只有最后 ~25 根有完整 MACD
- **修复**：`KLINE_COUNT 60 → 200`，33 根 seed + 167 根有效值，与常见看盘软件一致
- 检查点存储在 logs/.health-check-ts（ISO 时间戳），支持跨时段增量报告
- 睡眠时段静默执行并记入 notification-last.txt，清醒时段并在状态展示中呈现给用户
